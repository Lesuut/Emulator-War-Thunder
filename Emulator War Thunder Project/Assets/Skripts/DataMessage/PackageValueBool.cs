public class PackageValueBool : Package
{
    public PackageValueBool(string nameClient, string namePackage, TypeCrew typeCrew) : base(nameClient, namePackage, typeCrew)
    {
    }
    public bool Value;
}